<section class="hero-section w-full min-h-screen -mt-[4.5rem] lg:-mt-[5.5rem] pt-[4.5rem] lg:pt-[5.5rem] flex items-center justify-center relative">

  <!-- Imagen de fondo (primera obra de la galería) -->
  <div
    class="hero-bg"
    style="background-image: url('/assets/images/obras/1.webp');"
    aria-hidden="true"
  ></div>

  <!-- Overlay -->
  <div class="hero-overlay" aria-hidden="true"></div>

  <!-- Contenido -->
  <div class="relative z-10 w-full max-w-5xl mx-auto px-5 lg:px-10 py-16 lg:py-24 text-center text-white">

    <!-- Badge -->
    <div class="flex justify-center mb-5 reveal" style="animation-delay:0.1s">
      <span class="section-badge">
        <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 20 20">
          <path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"/>
        </svg>
        Empresa familiar · 20 años de trayectoria
      </span>
    </div>

    <!-- Título principal -->
    <h1 class="font-archivo font-extrabold text-3xl sm:text-4xl lg:text-6xl leading-tight mb-4 reveal reveal-delay-1">
      Proyectos<br class="hidden sm:block" />
      <span class="text-custom-original"> Llave en Mano</span>
    </h1>

    <!-- Subtítulo -->
    <p class="text-base sm:text-lg lg:text-xl text-white/80 max-w-2xl mx-auto mb-8 leading-relaxed reveal reveal-delay-2">
      Construimos hogares modernos y eficientes con Steel Framing e Isopanel.
      Tu proyecto desde el anteproyecto hasta la entrega de llaves.
    </p>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-14 reveal reveal-delay-3">
      <a class="btn-primary text-sm lg:text-base px-7 py-3.5" href="/#steelframing" id="hero-cta-proyectos">
        Ver proyectos
        <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
      <a class="btn-secondary text-sm lg:text-base px-7 py-3.5" href="/contacto" id="hero-cta-contacto">
        Contactanos
      </a>
    </div>

    <!-- Stats / Contadores -->
    <div class="grid grid-cols-3 gap-3 sm:gap-6 max-w-lg mx-auto reveal reveal-delay-4">
      <div class="text-center bg-white/10 backdrop-blur-sm rounded-2xl py-4 px-2 border border-white/15">
        <p class="font-archivo font-extrabold text-2xl sm:text-3xl lg:text-4xl text-white leading-none">
          <span class="stat-counter" data-target="120">0</span>+
        </p>
        <p class="text-white/65 text-xs sm:text-sm mt-1 uppercase tracking-wide font-semibold">Casas</p>
      </div>
      <div class="text-center bg-white/10 backdrop-blur-sm rounded-2xl py-4 px-2 border border-white/15">
        <p class="font-archivo font-extrabold text-2xl sm:text-3xl lg:text-4xl text-white leading-none">
          <span class="stat-counter" data-target="100">0</span>+
        </p>
        <p class="text-white/65 text-xs sm:text-sm mt-1 uppercase tracking-wide font-semibold">Reformas</p>
      </div>
      <div class="text-center bg-white/10 backdrop-blur-sm rounded-2xl py-4 px-2 border border-white/15">
        <p class="font-archivo font-extrabold text-2xl sm:text-3xl lg:text-4xl text-white leading-none">
          <span class="stat-counter" data-target="20">0</span>
        </p>
        <p class="text-white/65 text-xs sm:text-sm mt-1 uppercase tracking-wide font-semibold">Años</p>
      </div>
    </div>
  </div>

  <!-- Flecha scroll down -->
  <a
    href="/#steelframing"
    aria-label="Ver proyectos"
    class="absolute bottom-7 left-1/2 -translate-x-1/2 text-white/60 hover:text-white transition-colors"
    style="animation: pulse 2s ease-in-out infinite;"
  >
    <svg class="w-7 h-7" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/>
    </svg>
  </a>
</section>

<script is:inline>
  // Intersection Observer para animaciones reveal
  document.addEventListener("astro:page-load", () => {
    const reveals = document.querySelectorAll(".reveal");
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("visible");
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1 }
    );
    reveals.forEach((el) => observer.observe(el));

    // Contadores animados
    const counters = document.querySelectorAll(".stat-counter");
    const counterObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (!entry.isIntersecting) return;
          const el = entry.target;
          const target = parseInt(el.dataset.target, 10);
          const duration = 1200;
          const start = performance.now();

          function update(now) {
            const elapsed = now - start;
            const progress = Math.min(elapsed / duration, 1);
            // Ease-out cubic
            const eased = 1 - Math.pow(1 - progress, 3);
            el.textContent = Math.floor(eased * target);
            if (progress < 1) requestAnimationFrame(update);
            else el.textContent = target;
          }

          requestAnimationFrame(update);
          counterObserver.unobserve(el);
        });
      },
      { threshold: 0.5 }
    );
    counters.forEach((el) => counterObserver.observe(el));
  });
</script>
