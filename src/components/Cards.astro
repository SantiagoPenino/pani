---
interface Props {
  imgSrc: string;
  imgAlt: string;
  price: string;
  size: string;
  bedrooms: string;
  bathrooms: string;
}

const {imgSrc, imgAlt, price, size, bedrooms, bathrooms } = Astro.props;
---
<a href={`/productos/${imgSrc}`} class="block">
<div class="flex flex-col py-4 md:px-4 rounded transition-transform hover:scale-110 w-full md:w-80">
  <img class=" rounded-t-lg object-cover" src=`/productos/${imgSrc}.webp` alt={imgAlt} />
  <div
    class="flex  bg-custom-gray text-white justify-between p-3 rounded-b-lg"
  >
    <div>
      <p>{`U$S ${price}`}</p>
    </div>
    <div class="flex">
      <p>{`${size}m²`}</p>
      <div class="h-6 w-1 bg-custom-blue mx-2"></div>
      <p>{`${bedrooms}D`}</p>
      <div class="h-6 w-1 bg-custom-blue mx-2"></div>
      <p>{`${bathrooms}B`}</p>
    </div>
  </div>
</div>
</a>
